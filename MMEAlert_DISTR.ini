; Optional SPID support. Distribute our hidden event monitor broadly because
; MME Milk Maid status can change dynamically. The script performs its cheap
; validity and player-proximity checks only when a supported drink is equipped.
; NPC distribution is deliberately disabled during player-only testing.
;Spell = 0x000805~MMEAlert.esp|ActorTypeNPC
